# myMusic 项目交接手册

> 写于 2026-07-14，提交 `4a331f3` 之后（基于 2026-07-13 的手册更新）

## 目录

1. [项目概况](#1-项目概况)
2. [当前状态](#2-当前状态)
3. [开发环境](#3-开发环境)
4. [数据流：网络歌曲搜索与播放](#4-数据流网络歌曲搜索与播放)
5. [Redis 优化（已基本完成）](#5-redis-优化已基本完成)
6. [数据库说明](#6-数据库说明)
7. [关键改动记录（本周期）](#7-关键改动记录本周期)
8. [已知问题与待办](#8-已知问题与待办)
9. [关键文件索引](#9-关键文件索引)

---

## 1. 项目概况

一个在线音乐平台，Web 端 React 19 + Vite，后端 FastAPI + MySQL + Redis。支持搜索和播放网易云音乐歌曲，用户注册/登录、收藏歌单/歌曲/专辑、评论、私信等功能。

**仓库**: `https://github.com/Orlyi/myMusicRep`

**目录结构**：

```
D:\myMusic\
├── newMusic/          ← 后端 FastAPI（主力，Python）
├── web/               ← 前端 React 19 + Vite（主力，JSX）
├── mysql/SQL/         ← SQL 备份 + 迁移脚本
│   ├── create.sql     ← 最新建表语句（含优化改动）
│   ├── migration_optimize.sql       ← 第一批结构优化
│   ├── migration_optimize_2.sql     ← 第二批索引+FKCASCADE
│   └── procedures_triggers_functions.sql ← 存过+触发器+函数
├── spiders/           ← 爬虫脚本（调用 yinyueku.cn API，已弃用）
├── music/             ← 本地音乐文件（.mp3）
├── oldMusic/          ← 旧版 Java Spring Boot 后端（已废弃）
├── 建模/              ← PlantUML 建模图
├── 备份/              ← SQL 备份
├── memory/            ← Claude Code 持久记忆
└── 交接手册.md         ← 本文档
```

---

## 2. 当前状态

### 2.1 版本记录（git log）

```
4a331f3  Redis缓存+音频代理修复+数据库优化+SongDetail抽屉+歌手多对多+最近播放Redis  ← 当前 HEAD
b2bbe81  Redis基础设施 + 音频代理 + 下载修复
03144e9  前端 下滑线响应
ef0a5ad  Frontend init
19faa5c  init: web (React+Vite) + newMusic (FastAPI)
```

### 2.2 什么已经做完了

| 功能 | 后端 | 前端 | 数据库 |
|------|------|------|--------|
| 用户注册/登录(JWT) | ✅ | ✅ | ✅ |
| 歌曲搜索(本地DB) | ✅ | ❌（接口存在但无页面入口） | ✅ |
| 歌曲详情 | ✅ | ✅（SongDetail 底部抽屉） | ✅ |
| 歌手/专辑/歌单 | ✅ | ✅ | ✅ |
| 网络搜索(网易云实时) | ✅ | ✅（SearchPage） | ❌（不存） |
| **音频流代理(防403)** | ✅ | ✅ **已对接，自动走代理** | — |
| **Redis CDN缓存** | ✅ **已接入 play-url** | — | — |
| **Redis 最近播放（7天）** | ✅ | ✅ ProfilePage"听过"tab | — |
| **SongDetail 全局抽屉** | — | ✅ 所有EllipsisVertical绑定 | — |
| **歌手多对多** | ✅ | — | ✅ artist_sing_song |
| 收藏/关注/评论/私信 | ✅ | ✅ | ✅ |
| **自动补全元数据修复** | ✅ song-detail→album→artist | — | — |
| 歌词关联LYC | ✅ | — | ✅ |
| 后端下载 Worker | ❌ **未实现** | — | — |
| **DB 函数/触发器/存过** | — | — | ✅ 已安装 |

### 2.3 什么还未开始

- QQ 音乐源（接口定义了 `source=qq` 但未实现）
- Zustand 状态管理（Howler.js 已装）
- Capacitor 移动端打包
- 后台下载 Worker（独立进程）

---

## 3. 开发环境

### 3.1 启动方式

**后端**（在 `newMusic/` 目录下）：

```bash
# 1. 确保 MySQL 运行在 localhost:3306（用户 root，密码 sql2008）
#    数据库名 myMusic，字符集 utf8mb4

# 2. 确保 Redis 运行（WSL 或本地）
redis-server

# 3. 启动 FastAPI（端口 8000，带热重载）
cd D:/myMusic/newMusic
python app/main.py
```

**前端**：

```bash
cd D:/myMusic/web
npm run dev
# → http://localhost:5173
```

### 3.2 关键配置

| 文件 | 说明 |
|---|---|
| `newMusic/.env` | 数据库密码、JWT密钥、网易云Cookie、Redis地址 |
| `newMusic/app/core/config.py` | Settings 类（pydantic-settings 自动读 .env） |
| `newMusic/app/core/database.py` | 异步 MySQL 引擎（asyncmy 驱动） |
| `newMusic/app/core/redis_client.py` | 异步 Redis 连接池 |
| `web/src/api/client.js` | Axios 实例（baseURL: `http://localhost:8000/api/v1`） |

### 3.3 网易云 Cookie

`NETEASE_COOKIE` 在 `.env` 里，用于请求网易云播放地址（AES+RSA 加密解密需要有效的 Cookie 才能通过风控）。失效时去浏览器登录 music.163.com，F12 复制 Cookie 替换。

### 3.4 Redis

在 WSL Ubuntu 里装的。已在 WSL 终端验证 `redis-cli ping → PONG`。

```bash
# WSL 里
sudo service redis-server start
redis-cli ping   # → PONG
```

---

## 4. 数据流：网络歌曲搜索与播放

### 4.1 搜索流程（OK）

```
SearchPage.jsx
  → GET /api/v1/network/search?keyword=xxx&source=netease&type=song
  → NeteaseSearcher.search_song()
     → httpx GET https://music.163.com/api/search/get/web
     → 返回统一格式 NeteaseSearchResult[]
```

### 4.2 播放流程（已修复）

```
用户点击播放
  ↓
GET /api/v1/network/play-url?platform_id=xxx&source=netease&song_name=...&artist_names=...
  ↓
① 查 DB songs 表
  ├── 有本地文件 (/static/music/{id}.mp3) → ✅ 直接返回
  ├── 只有 CDN 链接 → 查 Redis 缓存 (netease:cdn_url:{platform_id})
  │   ├── Redis 有 → 返回缓存URL + is_cdn:true
  │   └── Redis 无 → 调网易云破解接口 → 存 Redis (TTL 10分钟) → 返回
  └── 无记录 → 调破解接口 → 自动补全元数据 → 入库 → 写 Redis → 下载到本地
  ↓
② 前端 resolveUrl() 判断：
  ├── 本地路径 → 直接播放
  └── CDN 链 → 自动走 /api/v1/network/audio-proxy?url=...（带Referer头中转）
```

### 4.3 自动补全元数据（已修复）

搜索返回的字段 `platform_id` 是**歌曲ID**。现在会：
1. 调 `/song-detail` 拿到真正的 `artists[0].id`（歌手ID）和 `album.id`（专辑ID）
2. 再分别调 `/artist-detail` 和 `/album-detail` 获取完整信息
3. 入库时三个表各存自己的 `platform_id`（不再全用歌曲ID）

### 4.4 最近播放（Redis 7天）

**数据流**：

```
playIndex() 播放
  ↓
recordListen(song.song_id)  ← fire-and-forget fetch POST /songs/{id}/listen
  ↓
Redis LPUSH → de-dupe → LTRIM(50) → EXPIRE(7天)
  ↓
ProfilePage "听过" → GET /users/me/recent-listens?page=1
  → Redis LRANGE → 查 songs/artists/albums 表完善信息 → 返回
```

---

## 5. Redis 优化（已基本完成）

### 5.1 CDN 缓存（已完成）

- `netease:cdn_url:{platform_id}` — 缓存网易云 CDN URL
- TTL: 600 秒（10 分钟）
- `/play-url` 接口写入和读取

### 5.2 最近播放（已完成）

- `user:listen:{user_id}` — Redis List
- 每次播放 LPUSH + LREM 去重 + LTRIM 50 条
- TTL: 604800 秒（7 天）

### 5.3 下载 Worker（待完成）

创建 `newMusic/app/services/download_worker.py`，从 Redis 队列消费下载任务。

### 5.4 架构图（当前状态）

```
用户点击播放
     │
     ▼
GET /play-url
     ├── MySQL 有本地路径 → 直接用
     ├── Redis 有 CDN 缓存 → 返回 URL（前端走代理）
     └── 都没有 → 调 Netease API → 存 Redis → 返回 CDN（代理播放）
     
前端 <audio>:
     ├── 本地路径 → 直接播
     └── CDN 链 → /audio-proxy?url=...（带Referer中转）
```

---

## 6. 数据库说明

### 6.1 关键表

| 表名 | 行数 | 说明 |
|------|------|------|
| `songs` | ~20 | 网络搜索后自动入库，本地文件存 `/static/music/` |
| `users` | ~10 | 测试用户 |
| 其他 | 少 | 收藏、评论、歌单等 |

### 6.2 songs 表核心字段

```sql
song_id        INT PRIMARY KEY AUTO_INCREMENT
platform_id    VARCHAR(200)     -- 网易云歌曲 ID
song_name      VARCHAR(200)     -- 歌曲名
artist_id      INT              -- FK → artists（主歌手）
album_id       INT              -- FK → albums
lyric_id       INT              -- FK → lyrics
picture_url    VARCHAR(500)     -- 封面图（已扩展长度）
source         VARCHAR(50)      -- 'netease'
download_url   VARCHAR(1000)    -- 本地路径 /static/music/{id}.mp3 或 CDN 链
```

### 6.3 已做的数据库优化

| 类型 | 内容 |
|------|------|
| Varchar 扩展 | picture_url→500, album_name→200, artist_name→100, user_name→50, email→100 |
| UNIQUE 约束 | `songs(platform_id,source)`, `lyrics(song_id)`, `users(email)` |
| 冗余删除 | 删 `user_details.index_user_id`, `lyrics.index_lyric_id`, 重复FK |
| 复合索引 | `play_history(user_id,create_time)`, `messages(receiver_id,sent_time)` 等 |
| FK 级联修复 | playlists/user_id CASCADE, comments/parent_id CASCADE, search_history/user_id CASCADE |
| 死表清理 | 删除无用 `album_save_song` |
| 函数 | `fn_get_song_artists(song_id)`, `fn_format_duration(ms)`, `fn_get_search_hot(keyword)` |
| 触发器 | 6个：自动维护 songs_count、like_count、update_time 等计数器 |
| 存储过程 | `sp_recalculate_all_counts()`, `sp_cleanup_stale_songs()` |

---

## 7. 关键改动记录（本周期）

| 改动 | 相关文件 | 说明 |
|------|---------|------|
| 音频代理 | `network_search.py` | `/audio-proxy` 端点，带 Referer 流转发 CDN 流 |
| Redis CDN 缓存 | `network_search.py`, `redis_keys.py` | play-url 接入 Redis |
| 自动补全修复 | `network_search.py` | song-detail → artist-detail + album-detail 正确顺序 |
| 歌手多对多 | `network_search.py` | 多歌手分创建 + 写入 artist_sing_song |
| SongDetail 抽屉 | `SongDetail.jsx`, `PlayerContext.jsx` | 全局底部抽屉，绑定所有 EllipsisVertical |
| 全局收藏 | `PlayerContext.jsx`, `PlayerBar.jsx`, `QueueDrawer.jsx`, `ProfilePage.jsx` | `lovedSet` + `toggleLove` 统一控制 |
| 跑马灯 | `PlayerBar.jsx`, `SongList.jsx`, `index.css` | 无缝循环滚动 |
| 最近播放 | `songs.py`, `users.py`, `PlayerContext.jsx` | Redis 7天 |
| DB 优化 | `create.sql`, `migration_optimize.sql` | 索引/FK/长度/约束 |

---

## 8. 已知问题与待办

### 8.1 💣 当前问题

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P0 | 网易云 Cookie 偶尔过期 | 需要重新浏览器登录复制 |
| P1 | DB 里旧 CDN 死链未清理 | 跑 `sp_cleanup_stale_songs()` |
| P1 | 首歌重复请求会多次调 Netease API | 未加 SETNX 去重（量小影响不大） |

### 8.2 📋 待办

- **后台下载 Worker** — 独立进程从 Redis 消费队列
- QQ 音乐源接入（`source=qq`）
- Howler.js 替代 `<audio>` 标签（已装 `web/package.json`）
- 本地搜索歌名端点返回歌手名（`search.py` join artist_sing_song）
- Zustand 状态管理重构
- Capacitor 打包 Android APK

---

## 9. 关键文件索引

### 后端（newMusic/app/）

| 文件 | 作用 | 重要 |
|------|------|------|
| `main.py` | 应用入口、生命周期 | ✅ |
| `core/config.py` | Settings 类 | ✅ |
| `core/database.py` | 异步 MySQL 引擎 | ✅ |
| `core/redis_client.py` | Redis 异步连接池 | ⭐ |
| `core/redis_keys.py` | Redis 键常量 | ⭐ |
| `api/v1/network_search.py` | 搜索+播放+代理（核心文件） | ⭐ |
| `api/v1/songs.py` | 本地歌曲 API（含 listen 端点） | ✅ |
| `api/v1/users.py` | 用户+最近播放 | ✅ |
| `api/v1/favorites.py` | 收藏 API | ✅ |
| `services/network_search_service/netease.py` | 网易云 API 调用+AES+RSA | ✅ |

### 前端（web/src/）

| 文件 | 作用 | 重要 |
|------|------|------|
| `layouts/PlayerContext.jsx` | 播放器状态（含 lovedSet/toggleLove/openDetail） | ⭐ |
| `components/SongDetail.jsx` | 全局底部抽屉 | ⭐ |
| `components/PlayerBar.jsx` | 底部播放条（含跑马灯） | ✅ |
| `components/SongList.jsx` | 队列歌单项（当前播放跑马灯） | ✅ |
| `components/SearchSongItem.jsx` | 搜索结果歌单项 | ✅ |
| `components/QueueDrawer.jsx` | 播放队列抽屉 | ✅ |
| `pages/SearchPage.jsx` | 网络搜索页面 | ✅ |
| `pages/ProfilePage.jsx` | 用户主页（收藏 + 听过 + 歌单） | ✅ |
| `pages/PlayerPage.jsx` | 全屏播放器（封面+歌词） | ✅ |

---

## 10. 首次接手的 Step-by-Step

```text
1. git clone https://github.com/Orlyi/myMusicRep
2. cd myMusic/newMusic && pip install -r requirements.txt
3. 确认 MySQL 运行，创建数据库 myMusic，source mysql/SQL/create.sql
4. 确认 .env 存在（复制并修改 NETEASE_COOKIE）
5. 启动 WSL Redis: `wsl sudo service redis-server start`
6. cd newMusic && python app/main.py    → 后端起在 localhost:8000
7. cd ../web && npm install && npm run dev  → 前端起在 localhost:5173
8. 打开浏览器，注册账号，登录
9. 点击搜索 → 搜"忽然之间" → 点播放 → 应该能响了
10. 如果播放 403 → 检查 Cookie（网易云，不是自己的）
```

---

> 最后更新: 2026-07-14 | 作者: Orly