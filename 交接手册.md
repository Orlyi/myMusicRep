# myMusic 项目交接手册

> 写于 2026-07-13，提交 `b2bbe81` 之后

## 目录

1. [项目概况](#1-项目概况)
2. [当前状态](#2-当前状态)
3. [开发环境](#3-开发环境)
4. [数据流：网络歌曲搜索与播放](#4-数据流网络歌曲搜索与播放)
5. [Redis 优化（待完成）](#5-redis-优化待完成)
6. [已知问题与待办](#6-已知问题与待办)
7. [数据库说明](#7-数据库说明)
8. [关键文件索引](#8-关键文件索引)

---

## 1. 项目概况

一个在线音乐平台，Web 端 React 19 + Vite，后端 FastAPI + MySQL。支持搜索和播放网易云音乐歌曲，用户注册/登录、收藏歌单/歌曲/专辑、评论、私信等功能。

**仓库**：`https://github.com/Orlyi/myMusicRep`

**目录结构**：

```
D:\myMusic\
├── newMusic/          ← 后端 FastAPI（主力，Python）
├── web/               ← 前端 React 19 + Vite（主力，JSX）
├── spiders/           ← 爬虫脚本（调用 yinyueku.cn API，已弃用）
├── music/             ← 本地音乐文件（.mp3）
├── oldMusic/          ← 旧版 Java Spring Boot 后端（已废弃）
├── 建模/              ← PlantUML 建模图
├── 备份/              ← SQL 备份
└── memory/            ← Claude Code 持久记忆（开机自动载入）
```

---

## 2. 当前状态

### 2.1 版本记录（git log）

```
b2bbe81  Redis基础设施 + 音频代理 + 下载修复     ← 当前 HEAD
03144e9  前端 下滑线响应
ef0a5ad  Frontend init
19faa5c  init: web (React+Vite) + newMusic (FastAPI)
```

### 2.2 什么已经做完了

| 功能 | 后端 | 前端 | 数据库 |
|------|------|------|--------|
| 用户注册/登录(JWT) | ✅ | ✅ | ✅ |
| 歌曲搜索(本地DB) | ✅ | ❌（接口存在但无页面入口） | ✅ |
| 歌曲详情 | ✅ | ✅ | ✅ |
| 歌手/专辑/歌单 | ✅ | ✅ | ✅ |
| 网络搜索(网易云实时) | ✅ | ✅（SearchPage） | ❌（不存） |
| 网络播放地址获取 | ✅ | ✅ | ✅（存 download_url） |
| **音频流代理(防盗链)** | ✅ **新增** | ❌ **未接** | — |
| 收藏/关注/评论/私信 | ✅ | ✅ | ✅ |
| Redis CDN缓存 | ✅ **基础设施就位** | ❌ **未接** | — |
| 后台下载Worker | ❌ **未实现** | — | — |
| 歌词关联LYC | ✅ | — | ✅ |

### 2.3 什么还未开始

- QQ 音乐源（接口定义了 `source=qq` 但未实现）
- Zustand 状态管理（Howler.js 已装）
- Capacitor 移动端打包
- 后台下载 Worker（独立进程）
- 前端搜索/播放与 Redis + 代理接口对接
- 现有数据库里全是网易云 CDN 死链，需要清理

---

## 3. 开发环境

### 3.1 启动方式

**后端**（先切到 bash 环境，在 `newMusic/` 目录下）：

```bash
# 1. 确保 MySQL 运行在 localhost:3306（用户 root，密码 sql2008）
#    数据库名 myMusic，字符集 utf8mb4

# 2. 启动 FastAPI（端口 8000，带热重载）
cd D:/myMusic/newMusic
python app/main.py

# 3. 如果需要 Redis（目前只需要启动，前端尚未对接）：
#    启动 Redis 服务器（无 Docker 时用 Windows 绿色版）
redis-server
```

**前端**：

```bash
cd D:/myMusic/web
npm run dev
# → http://localhost:5173
```

### 3.2 关键配置

| 文件 | 说明 |
|---|---|
| `newMusic/.env` | 数据库密码、JWT密钥、网易云Cookie、Redis地址 |
| `newMusic/app/core/config.py` | Settings 类（pydantic-settings 自动读 .env） |
| `newMusic/app/core/database.py` | 异步 MySQL 引擎（asyncmy 驱动） |
| `web/src/api/client.js` | Axios 实例（baseURL: `http://localhost:8000/api/v1`） |

### 3.3 网易云 Cookie

`NETEASE_COOKIE` 在 `.env` 里，用于请求网易云播放地址（AES+RSA 加密解密需要有效的 Cookie 才能通过风控）。失效时去浏览器登录 music.163.com，F12 复制 Cookie 替换。

### 3.4 确认安装的工具

```bash
pip install redis hiredis   # 已装好，用于 Redis 客户端
pip list | grep redis       # redis 8.0.1, hiredis 3.4.0
```

---

## 4. 数据流：网络歌曲搜索与播放

这是最核心也是最容易出问题的链路。

### 4.1 搜索流程（已经 OK）

```
SearchPage.jsx
  → GET /api/v1/network/search?keyword=xxx&source=netease&type=song
  → NeteaseSearcher.search_song()
     → httpx GET https://music.163.com/api/search/get/web
     → 返回统一格式 NeteaseSearchResult[]
```

### 4.2 播放流程（当前状态，部分修复但未完全解决）

```
用户点击播放
  ↓
GET /api/v1/network/play-url?platform_id=xxx&source=netease&...
  ↓
① 查 DB songs 表
  ├── 有记录且 download_url 是本地路径 (/static/music/{id}.mp3) → ✅ 直接返回
  ├── 有记录但 download_url 是 CDN 链接 (http://m701.music.126.net/...)
  │   ├── 检查 uploads/music/{id}.mp3 是否存在
  │   │   ├── 存在 → 更新 DB，返回本地路径
  │   │   └── 不存在 → 返回 CDN 链接（交给前端，但播放会 403）
  └── 无记录 → 调网易云破解接口 → 存入 DB → 异步下载到本地
  ↓
② 返回 play_url 给前端
  ↓
③ 前端 PlayerContext.jsx play() 接收 URL → 设到 <audio> src
  ├── 本地路径 (/static/...) → ✅ 正常播放
  └── CDN 路径 (http://m701.music.126.net/...) → ❌ 浏览器 403
```

### 4.3 当前致命问题

**`_download_mp3` 有了 Referer 头，但 CDN 会过期。**

网易云返回的 CDN URL 是**有时效性的**（约 10~20 分钟有效）。如果你第一次请求时已经过期，下载仍然会失败。**这就是数据库里所有 `download_url` 都是 CDN 链接、没有本地文件的原因**——CDN 链早就过期了，带 Referer 也下载不了。

**解决方案**（已设计，未完成）：

```
① 启动 Redis
② 前端接音频代理 /audio-proxy
   当 play_url 是 CDN 链时：
   <audio src="/api/v1/network/audio-proxy?url=编码后的CDN链">
   后端带 Referer 头无缝中转，不经过浏览器防盗链检查
③ 如果下载成功了，下次用它
   如果 Redis 里的 CDN 链没过期就直接透传，过期了自动再取
```

---

## 5. Redis 优化（待完成）

### 5.1 基础设施状态

Redis 客户端代码已就位，但**从未运行过**：

| 文件 | 内容 | 状态 |
|---|---|---|
| `newMusic/app/core/redis_client.py` | 异步连接池 `get_redis()` / `close_redis()` | ✅ 写好 |
| `newMusic/app/core/redis_keys.py` | 键前缀和 TTL 常量定义 | ✅ 写好 |
| `newMusic/app/core/config.py` | 加了 `redis_host/port/db/password` | ✅ 写好 |
| `.env` | 加了 `REDIS_HOST=localhost` 等 | ✅ 写好 |
| `requirements.txt` | 加了 `redis>=5.0` `hiredis>=2.0` | ✅ 已安装 |
| `app/main.py` | 生命周期里加了 `close_redis()` | ✅ 写好 |

### 5.2 还需要做的事

**① 安装 Redis 服务器**

Windows 环境，推荐：

```bash
# 方案 A: 用 Docker Desktop（但本机没有 Docker）
docker run -d --name mymusic-redis -p 6379:6379 redis:7-alpine

# 方案 B: winget 安装（但 winget 里没有标准的 Redis）
# 推荐去 https://github.com/tporadowski/redis/releases 下载 Redis-x64-5.0.14.1.zip
# 解压到 D:\myMusic\redis，双击 redis-server.exe
```

**② 改造 `/play-url` 接入 Redis**

```python
# 伪代码
redis = await get_redis()
cache_key = f"netease:cdn_url:{platform_id}"

cached = await redis.get(cache_key)
if cached:
    return cached  # 继续用

url = await network_get_play_url(...)
await redis.setex(cache_key, CDN_URL_TTL, url)  # TTL 600s
```

**③ 后端 Workers：下载队列**

创建 `newMusic/app/services/download_worker.py`：

```python
while True:
    # 从 Redis 弹任务
    task = await redis.rpop(DOWNLOAD_QUEUE_KEY)
    if not task:
        await asyncio.sleep(5)
        continue
    # 带 Referer 头下载 → 写 uploads/music/{id}.mp3
    # 更新 MySQL songs.download_url
```

启动方式：`python -m app.services.download_worker`

**④ 前端对接**

```js
// PlayerContext.jsx play() 函数
const isCdn = finalUrl.includes("music.126.net")
if (isCdn) {
  finalUrl = `/api/v1/network/audio-proxy?url=${encodeURIComponent(finalUrl)}`
}
audioRef.current.src = finalUrl
```

### 5.3 架构图（目标状态）

```
用户点击播放
     │
     ▼
GET /play-url
     ├── MySQL 有本地路径 → 直接用
     ├── Redis 有 CDN 缓存（TTL） → 用代理播放
     ├── 都没有 → 调 Netease API → 存 Redis → 写下载队列 → 返回 CDN（代理播放）
     │
同时：
     └── Download Worker（独立进程）
          ├── 从 Redis 取任务
          ├── 带 Referer 下载 CDN → 存本地
          └── 更新 MySQL download_url
```

---

## 6. 已知问题与待办

### 6.1 🔴 阻塞性问题

| 优先级 | 问题 | 影响 | 修复方向 |
|--------|------|------|----------|
| **P0** | 播放 403：DB 里的 CDN 链接全部过期 | 所有从网络搜索播放的歌都不响 | 前端接 `/audio-proxy` + 后端走 Redis 重新获取 |
| **P0** | DB 存量数据污染：下载过来就失败的歌曲，`download_url` 全是死链 | 重复请求时不会重新获取 | 启动一个一次性脚本清空或标记这些记录 |
| **P1** | 缺少 Redis 服务器 | Redis 代码无法验证 | 装 Redis 并启动 |

### 6.2 🟡 待优化

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P2 | CDN 链有过期时间但 DB 数据没有 TTL | 依赖 Redis 解决 |
| P2 | 下载阻塞 HTTP 响应 | 等 Worker 进程拆出来 |
| P2 | 同一首歌重复请求会多次调 Netease API | Redis SETNX 去重 |
| P3 | `play_url` 接口参数太多（6 个 Query） | 可以重构为 POST body |
| P3 | 歌词 LRC 没有单独端点 | `GET /lyric/{song_id}` 已经有了 |

### 6.3 🟢 后续功能

- QQ 音乐源接入
- Howler.js 替代 `<audio>` 标签（它已经被装进 `web/package.json`）
- Zustand 状态管理重构（项目初期就规划了但未落地）
- Capacitor 打包 Android APK

---

## 7. 数据库说明

### 7.1 关键表

| 表名 | 行数 | 说明 |
|------|------|------|
| `songs` | ~20 | 网络搜索后自动入库，`download_url` 全是 CDN 死链 |
| `users` | ~10 | 测试用户 |
| `user_details` | ~10 | 用户扩展信息 |
| 其他 | 少 | 收藏、评论、歌单等 |

### 7.2 `songs` 表核心字段

```sql
song_id        INT PRIMARY KEY AUTO_INCREMENT
platform_id    VARCHAR(200)     -- 网易云歌曲 ID
song_name      VARCHAR(200)     -- 歌曲名
artist_id      INT              -- FK → artists
album_id       INT              -- FK → albums
lyric_id       INT              -- FK → lyrics（歌词已入库的有）
picture_url    VARCHAR(200)     -- 封面图
source         VARCHAR(50)      -- 'netease'
download_url   VARCHAR(1000)    -- ❌ 全是死链
download_count INT              -- 未使用
play_count     INT              -- 未使用
```

### 7.3 注意

- `songs.picture_url` 也是第三方 CDN 链接（无防盗链），目前直接给前端 `<img>` 正常显示
- `lyrics` 表的 `lyric_text` 存 LRC 格式，`plain_lyric` 存纯文本（去时间戳）
- `artist_sing_song` 和 `album_save_song` 关联表存在但网络搜索保存歌曲时**没有同步写入**，只设了 `songs.artist_id` / `songs.album_id`

---

## 8. 关键文件索引

### 后端（newMusic/app/）

| 文件 | 作用 | 重要 |
|------|------|------|
| `main.py` | 应用入口、生命周期 | ✅ |
| `core/config.py` | Settings 类 | ✅ |
| `core/database.py` | 异步 MySQL 引擎 | ✅ |
| `core/redis_client.py` | Redis 异步连接池 | ⭐ 新 |
| `core/redis_keys.py` | Redis 键常量 | ⭐ 新 |
| `api/v1/network_search.py` | 网络搜索+播放地址+音频代理 | ⭐ 核心文件 |
| `api/v1/__init__.py` | 路由汇总 | ✅ |
| `services/network_search_service/netease.py` | 网易云 API 调用 | ✅ |
| `services/network_search_service/__init__.py` | 搜索入口封装 | ✅ |
| `models/songs.py` | Songs 模型 | ✅ |

### 前端（web/src/）

| 文件 | 作用 | 重要 |
|------|------|------|
| `layouts/PlayerContext.jsx` | 播放器状态管理（play/queue/lyrics） | ⭐ 需要改 |
| `pages/SearchPage.jsx` | 网络搜索页面 | ✅ |
| `api/search.js` | 搜索 API（含 play-url 请求） | ✅ |
| `pages/SongDetailPage.jsx` | 本地歌曲详情页 | ✅ |
| `components/PlayerBar.jsx` | 底部播放条 | ✅ |
| `components/QueueDrawer.jsx` | 播放队列抽屉 | ✅ |

---

## 9. 首次接手的 Step-by-Step

```text
1. git clone https://github.com/Orlyi/myMusicRep
2. cd myMusic/newMusic && pip install -r requirements.txt
3. 确认 MySQL 运行，创建数据库 myMusic，导入 备份/*.sql
4. 确认 .env 存在（复制并修改）
5. python app/main.py    → 后端起在 localhost:8000
6. cd ../web && npm install && npm run dev
                      → 前端起在 localhost:5173
7. 打开浏览器，注册一个账号，登录
8. 点击搜索 → 搜索"忽然之间" → 搜索结果出现
9. 点击播放  → 后端日志看到请求 /play-url
   → 返回 CDN URL → 浏览器 <audio> 播放 403
10. 到这里说明环境正常，开始修播放问题
```

---

## 10. 本手册不是一次性文档

只要项目有重大变更——新增模块、数据库迁移、第三方依赖增加、架构调整——就应该更新这个文件。接手的人靠它恢复上下文。

> 最后更新: 2026-07-13 | 作者: Orly
