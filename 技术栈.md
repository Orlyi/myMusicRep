# myMusic 前端技术栈

## 整体架构

```
D:\myMusic\
├── shared/          # 共享层（类型 + API + 状态管理 + 工具）
├── web/             # Web 端（React SPA）
├── music/           # 本地音乐文件
├── newMusic/        # 后端 FastAPI
└── spiders/         # 爬虫
```

## 技术栈

### Web 端 (`web/`)
| 项 | 选型                             |
|---|--------------------------------|
| 框架 | React 19                       |
| 构建 | Vite（SPA，非 SSR）                |
| 路由 | React Router v7                |
| 状态管理 | Zustand（先用原生react管理，后面在考虑是否使用） |
| HTTP | axios                          |
| 样式 | 传统css，(不用TailwindCSS v4)       |
| 音频 | Howler.js                      |
| 图标 | Lucide React                   |

### 移动端 App（基于 `web/` 打包）
| 项 | 选型 |
|---|---|
| 方案 | Capacitor（免费开源，MIT 协议） |
| 原理 | 把 web/ 的 build 产物打包成 Android APK / iOS IPA |
| 原生功能 | Capacitor 插件（音频、存储、通知等） |
| 平台检测 | `@capacitor/core` 的 `Capacitor.isNativePlatform()` |
| 布局策略 | 同一份组件结构 + TailwindCSS 响应式 + 平台条件判断 |

> **不写两套 UI**。Web 和 App 共用 `web/` 的全部代码，仅通过 CSS 响应式断点 + 平台检测切换布局差异。

### 共享层 (`shared/`)
| 项 | 说明 |
|---|---|
| types/ | TypeScript 类型定义（对应后端 Pydantic schema） |
| api/ | axios 封装 + 按模块拆分的 API 函数 |
| stores/ | Zustand store（auth、player、queue） |
| utils/ | token 管理、格式化等 |

### 后端 (`newMusic/`)
| 项 | 选型 |
|---|---|
| 框架 | FastAPI（Python） |
| 数据库 | MySQL + SQLAlchemy 2.0 async + asyncmy |
| 迁移 | Alembic |
| 认证 | JWT + bcrypt |

## 关键决策

- **Web + App 同一套代码**：React + Vite 写页面，Capacitor 打包成 App，不建独立的 mobile 项目
- **SPA 非 SSR**：音乐 App 不需要 SEO，纯浏览器端渲染即可
- **播放队列放前端**：Zustand `queueStore` 管理，不调后端接口
- **先开发 Android**：Windows 环境，iOS 需要 Mac 才能编译
- **JWT 密钥开发阶段不改**：`change-me-to-a-random-string` 先用着
- **布局适配**：TailwindCSS 响应式断点（`md:` `lg:`）+ Capacitor 平台检测双管齐下
  - **版本管理** git仓库 已完成 地址为https://github.com/Orlyi/myMusicRep

## 页面规划（Web + App 共用）

| 路由 | 页面 |
|------|------|
| `/` | 首页（热门歌曲） |
| `/search` | 搜索（歌曲/歌手/专辑/歌词） |
| `/song/:id` | 歌曲详情 + 评论 |
| `/artist/:id` | 歌手详情 |
| `/album/:id` | 专辑详情 |
| `/playlist/:id` | 歌单详情 |
| `/login` | 登录 |
| `/register` | 注册 |
| `/library` | 我的收藏/歌单/历史 |
| `/messages` | 私信 |
| `/profile` | 个人信息 |

## 后端 API 端点（参考）

```
/api/v1/auth/login         POST   登录
/api/v1/auth/register      POST   注册
/api/v1/auth/me            GET    当前用户
/api/v1/songs              GET    歌曲列表
/api/v1/songs/:id          GET    歌曲详情
/api/v1/songs/:id/play     POST   播放记录/下载
/api/v1/artists            GET    歌手列表
/api/v1/artists/:id        GET    歌手详情
/api/v1/albums             GET    专辑列表
/api/v1/albums/:id         GET    专辑详情
/api/v1/playlists          GET/POST    歌单列表/创建
/api/v1/playlists/:id      GET/PUT/DELETE  歌单详情/修改/删除
/api/v1/search/:type       GET    搜索（song/artist/album/lyric）
/api/v1/comments           GET/POST   评论列表/发表
/api/v1/favorites          GET/POST/DELETE  收藏
/api/v1/follow             POST/DELETE  关注/取关
/api/v1/message            GET/POST/DELETE  私信
/api/v1/users/me/info      GET    用户详情
/api/v1/users/me/history   GET    播放历史
```
